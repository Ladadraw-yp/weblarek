# Проектная работа "Веб-ларек"

Стек: HTML, SCSS, TS, Vite

Структура проекта:
- src/ — исходные файлы проекта
- src/components/ — папка с JS компонентами
- src/components/base/ — папка с базовым кодом

Важные файлы:
- index.html — HTML-файл главной страницы
- src/types/index.ts — файл с типами
- src/main.ts — точка входа приложения
- src/scss/styles.scss — корневой файл стилей
- src/utils/constants.ts — файл с константами
- src/utils/utils.ts — файл с утилитами

## Установка и запуск
Для установки и запуска проекта необходимо выполнить команды

```
npm install
npm run dev
```

или

```
yarn
yarn dev
```
## Сборка

```
npm run build
```

или

```
yarn build
```
# Интернет-магазин «Web-Larёk»
«Web-Larёk» — это интернет-магазин с товарами для веб-разработчиков, где пользователи могут просматривать товары, добавлять их в корзину и оформлять заказы. Сайт предоставляет удобный интерфейс с модальными окнами для просмотра деталей товаров, управления корзиной и выбора способа оплаты, обеспечивая полный цикл покупки с отправкой заказов на сервер.

## Архитектура приложения

Код приложения разделен на слои согласно парадигме MVP (Model-View-Presenter), которая обеспечивает четкое разделение ответственности между классами слоев Model и View. Каждый слой несет свой смысл и ответственность:

Model - слой данных, отвечает за хранение и изменение данных.  
View - слой представления, отвечает за отображение данных на странице.  
Presenter - презентер содержит основную логику приложения и  отвечает за связь представления и данных.

Взаимодействие между классами обеспечивается использованием событийно-ориентированного подхода. Модели и Представления генерируют события при изменении данных или взаимодействии пользователя с приложением, а Презентер обрабатывает эти события используя методы как Моделей, так и Представлений.

### Базовый код

#### Класс Component
Является базовым классом для всех компонентов интерфейса.
Класс является дженериком и принимает в переменной `T` тип данных, которые могут быть переданы в метод `render` для отображения.

Конструктор:  
`constructor(container: HTMLElement)` - принимает ссылку на DOM элемент за отображение, которого он отвечает.

Поля класса:  
`container: HTMLElement` - поле для хранения корневого DOM элемента компонента.

Методы класса:  
`render(data?: Partial<T>): HTMLElement` - Главный метод класса. Он принимает данные, которые необходимо отобразить в интерфейсе, записывает эти данные в поля класса и возвращает ссылку на DOM-элемент. Предполагается, что в классах, которые будут наследоваться от `Component` будут реализованы сеттеры для полей с данными, которые будут вызываться в момент вызова `render` и записывать данные в необходимые DOM элементы.  
`setImage(element: HTMLImageElement, src: string, alt?: string): void` - утилитарный метод для модификации DOM-элементов `<img>`


#### Класс Api
Содержит в себе базовую логику отправки запросов.

Конструктор:  
`constructor(baseUrl: string, options: RequestInit = {})` - В конструктор передается базовый адрес сервера и опциональный объект с заголовками запросов.

Поля класса:  
`baseUrl: string` - базовый адрес сервера  
`options: RequestInit` - объект с заголовками, которые будут использованы для запросов.

Методы:  
`get(uri: string): Promise<object>` - выполняет GET запрос на переданный в параметрах ендпоинт и возвращает промис с объектом, которым ответил сервер  
`post(uri: string, data: object, method: ApiPostMethods = 'POST'): Promise<object>` - принимает объект с данными, которые будут переданы в JSON в теле запроса, и отправляет эти данные на ендпоинт переданный как параметр при вызове метода. По умолчанию выполняется `POST` запрос, но метод запроса может быть переопределен заданием третьего параметра при вызове.  
`handleResponse(response: Response): Promise<object>` - защищенный метод проверяющий ответ сервера на корректность и возвращающий объект с данными полученный от сервера или отклоненный промис, в случае некорректных данных.

#### Класс EventEmitter
Брокер событий реализует паттерн "Наблюдатель", позволяющий отправлять события и подписываться на события, происходящие в системе. Класс используется для связи слоя данных и представления.

Конструктор класса не принимает параметров.

Поля класса:  
`_events: Map<string | RegExp, Set<Function>>)` -  хранит коллекцию подписок на события. Ключи коллекции - названия событий или регулярное выражение, значения - коллекция функций обработчиков, которые будут вызваны при срабатывании события.

Методы класса:  
`on<T extends object>(event: EventName, callback: (data: T) => void): void` - подписка на событие, принимает название события и функцию обработчик.  
`emit<T extends object>(event: string, data?: T): void` - инициализация события. При вызове события в метод передается название события и объект с данными, который будет использован как аргумент для вызова обработчика.  
`trigger<T extends object>(event: string, context?: Partial<T>): (data: T) => void` - возвращает функцию, при вызове которой инициализируется требуемое в параметрах событие с передачей в него данных из второго параметра.

## Данные
В приложении используются структуры данных, описывающие основные сущности предметной области. Для их типизации и упрощения работы с данными используются интерфейсы.
### Интерфейс товара (IProduct)
Интерфейс IProduct описывает товар, который может быть представлен в каталоге и добавлен в корзину.
```
interface IProduct {
  id: string;
  description: string;
  image: string;
  title: string;
  category: string;
  price: number | null;
}
```
Назначение полей:

`id: string` — уникальный идентификатор товара.   
`description: string` — текстовое описание товара.   
`image: string` — ссылка на изображение товара.   
`title: string` — название товара.    
`category: string` — категория, к которой относится товар.    
`price: number | null` — цена товара. Может быть null, если цена отсутствует. 


### Интерфейс покупателя (IBuyer)
Интерфейс IBuyer описывает данные покупателя, необходимые для оформления заказа.
```
interface IBuyer {
  payment: TPayment;
  address: string;
  email: string;
  phone: string;
}
```

Назначение полей:

`payment: TPayment` — выбранный способ оплаты.    
`email: string` — электронная почта покупателя.   
`phone: string` — номер телефона покупателя.    
`address: string` — адрес доставки.


### Тип способа оплаты (TPayment)
Тип TPayment используется для описания выбранного покупателем способа оплаты.
Он может принимать одно из фиксированных строковых значений либо быть пустым, если способ оплаты ещё не выбран.
```
type TPayment = 'online' | 'onDelivery' | null;
```
Возможные значения:
- 'online' — оплата онлайн;
- 'onDelivery' — оплата при получении;
- null — способ оплаты не выбран.
Тип TPayment используется в интерфейсе IBuyer, а также в модели данных покупателя для хранения и валидации информации о способе оплаты.


## Моделях данных
Для работы с данными в приложении используются модели данных. Каждая модель отвечает за свою зону ответственности и инкапсулирует логику хранения и обработки данных.

### Каталог товаров (Catalog)
Класс Catalog отвечает за:
- хранение списка всех доступных товаров;
- хранение товара, выбранного для подробного отображения;
- предоставление методов для получения и поиска товаров.

Конструктор   
`constructor()`   
Конструктор не принимает параметров и инициализирует внутренние поля класса.


Поля класса   
`products: IProduct[]` — массив всех товаров, доступных в приложении.   
`selectedProduct: IProduct | null` — товар, выбранный для подробного просмотра.   

Методы класса   
`setProducts(products: IProduct[]): void` - cохраняет массив товаров в модели.    
`getProducts(): IProduct[]` - возвращает массив всех товаров.   
`getProductById(id: string): IProduct | undefined` - принимает id товара и возвращает найденный товар либо undefined, если товар не найден.   
`setSelectedProduct(product: IProduct): void` - сохраняет товар для подробного отображения.   
`getSelectedProduct(): IProduct | null` - возвращает товар, выбранный для подробного отображения.   


### Корзина (Basket)
Класс Basket отвечает за:
- хранение товаров, выбранных пользователем для покупки;
- управление содержимым корзины;
- расчет количества и стоимости товаров.

Конструктор   
`constructor()` - конструктор инициализирует пустую корзину.    

Поля класса   
`items: IProduct[]` — массив товаров, добавленных в корзину.    

Методы класса   
`getItems(): IProduct[]` - возвращает массив товаров в корзине.   
`addItem(product: IProduct): void` - добавляет товар в корзину.   
`removeItem(product: IProduct): void` - удаляет переданный товар из корзины.    
`clear(): void` - полностью очищает корзину.    
`getTotalPrice(): number` - возвращает суммарную стоимость всех товаров в корзине.    
`getItemsCount(): number` - возвращает количество товаров в корзине.    
`hasItem(id: string): boolean` - проверяет наличие товара в корзине по его id.    


### Покупатель (Buyer)
Класс Buyer отвечает за:
- хранение данных покупателя;
- обновление данных по отдельным полям;
- очистку данных;
- валидацию введённых значений.

Конструктор   
`constructor()` - конструктор инициализирует поля покупателя пустыми значениями.    

Поля класса   
`payment: TPayment` — выбранный способ оплаты.   
`address: string` — адрес доставки.   
`phone: string` — номер телефона.   
`email: string` — электронная почта.    

Методы класса   
`setData(data: Partial<IBuyer>): void` - сохраняет данные покупателя. Позволяет обновлять отдельные поля, не затирая уже сохранённые значения.    
`getData(): IBuyer` - возвращает все данные покупателя.   
`clear(): void` - очищает все данные покупателя.    
`validate(): Partial<Record<keyof IBuyer, string>>` - выполняет валидацию данных покупателя.    
Возвращает объект с ошибками, где ключ — имя поля, а значение — текст ошибки.
Если поле валидно, оно отсутствует в возвращаемом объекте.    
Пример результата валидации:
```
{
  payment: 'Не выбран вид оплаты',
  email: 'Укажите email'
}
```

## Слой коммуникации
### Класс ApiService
Отвечает за получение данных с сервера и отправку данных на сервер.

Конструктор    
`constructor(api: IApi)` — принимает экземпляр класса, реализующего интерфейс IApi.

Поля:   
`api: IApi` - экземпляр API-клиента для выполнения запросов

Методы класса   
`getProductList(): Promise<IProduct[]>` -  выполняет GET-запрос для получения каталога товаров с сервера.
`createOrder(order: TOrder): Promise<object>` - выполняет POST-запрос для отправки данных заказа на сервер.


## Слой представления (View)
Отвечает за отображение данных и обработку пользовательских действий.
Каждый класс представления управляет одним логическим блоком разметки и не содержит бизнес-логики.
Все классы слоя View:
- получают данные извне (от Презентера);
- генерируют события в ответ на действия пользователя;
- используют EventEmitter для уведомления Презентера.

### Gallery
Компонент слоя View, предназначенный для отображения каталога товаров на главной странице приложения.
Отвечает за вывод списка карточек товаров в контейнере галереи.

Конструктор
`constructor(container: HTMLElement)` 

Методы
`render(items: HTMLElement[]): HTMLElement` - Полностью заменяет содержимое контейнера галереи переданными элементами
`items` — массив DOM-элементов карточек товаров


### Header
Компонент слоя View, предназначенный для отображения шапки сайта.
Отвечает за отображение иконки корзины и счётчика количества товаров в корзине, а также за обработку клика по корзине.

Конструктор
`constructor(events: IEvents, container: HTMLElement)`

Поля
`protected basketButton: HTMLButtonElement` — кнопка открытия корзины
`protected counterElement: HTMLElement` — элемент отображения количества товаров в корзине

Методы
`set counter(value: number)` - обновляет текстовое содержимое счётчика корзины
`value` — количество товаров в корзине

Интерфейс
```
interface HeaderData {
	counter: number;
}
```

### Card
Назначение
Компонент слоя View, предназначенный для отображения одного товара в каталоге.
Отвечает за вывод информации о товаре и генерацию события выбора карточки пользователем.

Конструктор
`constructor(events: IEvents, container: HTMLElement)`

Поля

`protected titleElement: HTMLElement` — элемент отображения названия товара
`protected categoryElement: HTMLElement` — элемент отображения категории товара
`protected imageElement: HTMLImageElement` — элемент изображения товара
`protected priceElement: HTMLElement` — элемент отображения цены товара или статуса доступности

Методы
`render(data: CardData): HTMLElement` - обновляет содержимое карточки в соответствии с переданными данными

Интерфейс
```
interface CardData {
	id: string;
	title: string;
	category: string;
	image: string;
	price: number | null;
}
```
Интерфейс описывает набор данных, необходимых для отображения карточки товара в каталоге.

### CatalogCard
Назначение
Компонент слоя View, предназначенный для карточки товара в каталоге. 
Наследуется от базового класса `Card` и расширяет его функциональностью отображения изображения товара, категории и обработки кликов.

Конструктор
`onstructor(container: HTMLElement, action?: ICardActions)`

Поля
`cardImage: HTMLImageElement` - элемент изображения товара
`cardCategory: HTMLElement` - элемент отображения категории товара

Методы 
`image(src: string)` - устанавливает изображение товара.
`category(value: string)` - устанавливает категорию товара и применяет соответствующий класс стилей из `categoryMap`

Интерфейсы
```
interface ICardActions {
  onClick: (event: MouseEvent) => void;
}
```
Обработчик клика по карточке.
```
interface ICatalogCard {
  image: string;
  category: string;
}
```
Описывает структуру данных карточки каталога

### PreviewCard
Назначение
Класс PreviewCard представляет собой компонент карточки предпросмотра товара с расширенной информацией. Наследуется от базового класса Card и расширяет его функциональностью отображения изображения, категории, описания товара и интерактивной кнопки.

Конструктор
`constructor(container: HTMLElement, onClick: () => void)`

Поля
`imageCard: HTMLImageElement` - элемент изображения товара
`categoryCard: HTMLElement` - элемент отображения категории товара
`descriptionCard: HTMLElement` - элемент описания товара
`buttonCard: HTMLButtonElement` - кнопка действия 

Методы
`image(value: string)` - устанавливает изображение товара с добавлением пути к CDN и альтернативным текстом
`category(value: string)` - устанавливает категорию товара и применяет соответствующий класс стилей из categoryMap
`description(value: string)` - устанавливает текстовое описание товара
`buttonText(value: string)` - устанавливает текст на кнопке
`buttonActive(value: boolean)` - активирует/деактивирует кнопку (устанавливает disabled)

Интерфейсы
```
interface IPreviewCard extends ICatalogCard {
  description: string;
  buttonActive: boolean;
  buttonText: string;
}
```
Описывает структуру данных карточки просмотра товара.



### BasketCard
Назначение
Класс BasketCard представляет собой компонент карточки товара в корзине покупок. Наследуется от базового класса Card и расширяет его функциональностью отображения порядкового номера товара и кнопки удаления.

Конструктор
`constructor(container: HTMLElement, onClick: () => void)`

Поля
`indexCard: HTMLElement` - элемент отображения порядкового номера товара в корзине
`deleteButton: HTMLButtonElement` - Кнопка удаления товара из корзины

Методы
`index(value: number)` - устанавливает порядковый номер товара в корзине

Интерфейсы
```
interface IBasketCard extends ICard {
    index: number;
}
```
Описывает структуру данных карточки товара в корзине.

### Modal
Назначение
Класс Modal представляет собой модальное окно с возможностью закрытия по клику на кнопку закрытия, по клику на оверлей (затемнённую область) и по нажатию клавиши Escape.

Конструктор
`constructor(container: HTMLElement, events: IEvents)`

Поля
`closeButton: HTMLButtonElement` - кнопка закрытия модального окна (крестик)
`cntentInModal:HTMLElement` - контейнер для контента внутри модального окна

Методы
`content(value: HTMLElement)` - устанавливает контент модального окна, заменяя дочерние элементы
`open(): void` - открывает модальное окно, добавляет класс активности и слушатель клавиатуры
`close(): void` - закрывает модальное окно, удаляет класс активности, слушатель клавиатуры и очищает контент
`handleEscUp(evt: KeyboardEvent): void` - обработчик нажатия клавиши Escape для закрытия окна

Интерфейсы
```
interface IModal {
  content: HTMLElement;
}
```
Описывает структуру данных модального окна с контентом.


### Form
Назначение
Класс Form представляет собой абстрактный базовый класс для работы с формами. Предоставляет общую функциональность валидации, отображения ошибок и обработки отправки формы. Наследуется от базового класса Component.

Конструктор
`constructor(events: IEvents, container: HTMLElement)`

Поля
`formError: HTMLElement` - элемент для отображения ошибок формы
`formButton: HTMLButtonElement` - кнопка отправки формы (submit)

Методы
`error(value: string)` - устанавливает текст ошибки валидации формы
`valid(value: boolean)` - устанавливает валидность формы. При false кнопка отправки блокируется

Интерфейсы
```
interface IForm {
  error?: string;
  valid?: boolean;
}
```
Описывает структуру данных формы с полями ошибки и валидности.



### OrderForm
Назначение
Класс OrderForm представляет собой форму заказа с выбором способа оплаты и вводом адреса доставки. Наследуется от базового класса Form и расширяет его функциональностью обработки способов оплаты и адреса.

Конструктор
`constructor(container: HTMLElement, protected events: IEvents)`

Поля
`formOrderButtons: HTMLButtonElement[]` - массив кнопок выбора способа оплаты
`formAddress: HTMLInputElement` - поле ввода адреса доставки

Методы
`payment(value: TPayment)` - устанавливает активную кнопку способа оплаты, добавляя/удаляя класс button_alt-active
`address(value: string)` - устанавливает значение поля адреса доставки

Интерфейсы
```
interface IOrderForm {
  payment?: TPayment;
  address?: string;
}
```
Описывает структуру данных формы заказа


### ContactsForm
Назначение
Класс ContactsForm представляет собой форму ввода контактных данных покупателя (электронная почта и телефон). Наследуется от базового класса Form и расширяет его функциональностью обработки полей контактов.

Конструктор
`constructor(container: HTMLElement, protected events: IEvents)`

Поля
`formEmail: HTMLInputElement` - поле ввода электронной почты
`formPhone: HTMLInputElement` - поле ввода номера телефона

Методы
`set email(value: string)` - устанавливает значение поля электронной почты
`set phone(value: string)` - устанавливает значение поля номера телефона

Интерфейсы
```
interface IContactsForm {
  email?: string;
  phone?: string;
}
```
Описывает структуру данных формы контактов.



### BasketView
Назначение
Класс BasketView представляет собой компонент отображения корзины покупок с перечнем товаров, общей суммой и кнопкой оформления заказа. Наследуется от базового класса Component.

Конструктор
`constructor(container: HTMLElement, private readonly events: IEvents)`

Поля
`listElement: HTMLElement` - элемент списка товаров в корзине
`totalElement: HTMLElement` - элемент отображения общей суммы
`button: HTMLButtonElement` - кнопка оформления заказа

Методы
`items(items: HTMLElement[])` - устанавливает список карточек товаров. Если список пуст — очищает контейнер и блокирует кнопку
`total(value: number)` - устанавливает общую сумму заказа с суффиксом "синапсов"

Интерфейсы
```
interface IBasketView {
  items: HTMLElement[];
  total: number;
}
```
Описывает структуру данных представления корзины.

### Success
Назначение
Класс Success представляет собой компонент модального окна успешного завершения покупки с отображением суммы списанных средств и кнопкой закрытия.

Конструктор
`constructor(container: HTMLElement, protected events: IEvents)`

Поля
`description: HTMLElement` - элемент отображения описания (суммы списанных средств)
`closeButton: HTMLButtonElement` - кнопка закрытия окна

Методы
`set total(value: number)` - устанавливает текст описания с указанной суммой списанных синапсов

Интерфейсы
```
interface ISuccess {
  total: number;
}
```
Описывает структуру данных окна успешного завершения покупки.


## Презентер 
Презентер реализован в файле main.ts и является связующим звеном между Model и View.

### Список событий проекта

#### События каталога товаров
`catalog:changed` - Обновление списка товаров в каталоге. Формирует карточки товаров и отображает их в галерее
`card:select`  - Выбор карточки товара пользователем
`product:changed` - Изменение выбранного продукта для предпросмотра

#### События корзины
`basket:changed` - Изменение состояния корзины (добавление/удаление товара)
`basket:open` - Открытие корзины покупок
`basket:remove` - Удаление товара из корзины
`basket:checkout` - Начало оформления заказа


#### События предпросмотра товара
`preview:action`
Действие с товаром (добавить/убрать из корзины)


#### События форм
`form:changed` - Изменение поля формы
`order:submit` - Отправка формы заказа
`contacts:submit` - Отправка формы контактов


#### События успешного завершения
`order:success` - Успешное завершение заказа
`success:close` - Закрытие окна успеха
`modal:open` - Открытие модального окна
`modal:close` - Закрытие модального окна
`userdata:clear` - Очистка пользовательских данных 
